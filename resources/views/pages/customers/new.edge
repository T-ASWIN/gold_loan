<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Add New Customer</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="bg-slate-50 min-h-screen py-12">

    <div class="max-w-md mx-auto p-4 border rounded-2xl shadow-sm"
        style="background-color: #eeefef; border-color: #65696e;">

        <div class="flex justify-between items-end mb-4">
            <div class="space-y-1">
                <span class="text-xl font-bold uppercase text-slate-600">Registration</span>
                <h1 class="text-3xl font-extrabold tracking-tight text-slate-900">
                    New Customer
                </h1>
            </div>
            <a href="{{ route('customer.index') }}"
                class="text-sm font-medium text-slate-500 hover:text-slate-800 transition-colors">
                Cancel
            </a>
        </div>

        <form action="{{ route('customer.store') }}" method="POST" enctype="multipart/form-data" class="space-y-6">
            {{ csrfField() }}

            <div class="space-y-2">
                <label for="name" class="block text-sm font-semibold text-slate-700">Full Name</label>
                <input type="text" name="name" id="name" value="{{ old('name') || '' }}" required placeholder="John Doe"
                    class="block w-full px-4 py-3 rounded-xl border-slate-200 text-slate-900 focus:ring-2 focus:ring-slate-900 focus:border-slate-900 transition-all placeholder:text-slate-400 shadow-sm">
                @if(flashMessages.has('errors.name'))
                <p class="text-xs font-bold text-red-600 mt-1">{{ flashMessages.get('errors.name') }}</p>
                @end
            </div>

            <div class="space-y-2">
                <label for="email" class="block text-sm font-semibold text-slate-700">Email Address</label>
                <input type="email" name="email" id="email" value="{{ old('email') || '' }}" required
                    placeholder="john@example.com"
                    class="block w-full px-4 py-3 rounded-xl border-slate-200 text-slate-900 focus:ring-2 focus:ring-slate-900 focus:border-slate-900 transition-all placeholder:text-slate-400 shadow-sm">
                @if(flashMessages.has('errors.email'))
                <p class="text-xs font-bold text-red-600 mt-1">{{ flashMessages.get('errors.email') }}</p>
                @end
            </div>

            <div class="space-y-2">
                <label for="phoneNumber" class="block text-sm font-semibold text-slate-700">Phone Number</label>
                <input type="text" name="phoneNumber" id="phoneNumber" value="{{ old('phoneNumber') || '' }}"
                    placeholder="+1 (555) 000-0000"
                    class="block w-full px-4 py-3 rounded-xl border-slate-200 text-slate-900 focus:ring-2 focus:ring-slate-900 focus:border-slate-900 transition-all placeholder:text-slate-400 shadow-sm">
            </div>

            <div class="space-y-2">
                <label for="address" class="block text-sm font-semibold text-slate-700">Physical Address</label>
                <textarea name="address" id="address" rows="3" placeholder="Street, City, Country"
                    class="block w-full px-4 py-3 rounded-xl border-slate-200 text-slate-900 focus:ring-2 focus:ring-slate-900 focus:border-slate-900 transition-all placeholder:text-slate-400 shadow-sm">{{ old('address') || '' }}</textarea>
            </div>

            <div class="space-y-2">
                <label for="pledgecard" class="block text-sm font-semibold text-slate-700">Pledge Card</label>
                <div class="relative">
                    <input type="file" name="pledgecard" id="pledgecard" class="block w-full text-sm text-slate-500
                                      file:mr-4 file:py-2 file:px-4
                                      file:rounded-full file:border-0
                                      file:text-sm file:font-semibold
                                      file:bg-slate-900 file:text-white
                                      hover:file:bg-slate-800
                                      cursor-pointer">
                </div>
                @if(flashMessages.has('errors.pledgecard'))
                <p class="text-xs font-bold text-red-600 mt-1">{{ flashMessages.get('errors.pledgecard') }}</p>
                @end
            </div>

            <div class="pt-2">
                <button type="submit"
                    class="w-full bg-slate-900 hover:bg-slate-800 text-white py-4 px-4 rounded-xl font-bold shadow-lg shadow-slate-200 transition-all active:scale-[0.98] cursor-pointer">
                    Create Customer
                </button>
            </div>
        </form>
    </div>

</body>

</html>