<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Gold Loan System</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">

    <!-- Header -->
    <header class="bg-gray-400 shadow-sm h-14 flex items-center px-6 justify-between fixed top-0 left-0 right-0 z-20">
        <span class="text-white text-sm font-medium">FGLP Lead</span>

        <!-- Auth Section (right side) -->
        <div class="flex items-center gap-3 text-sm">
            @if(auth.isAuthenticated)
            <span class="text-white truncate max-w-xs">
                {{ auth.user.fullName ?? auth.user.email }}
            </span>
            <form action="{{ route('auth.logout') }}" method="POST">
                {{ csrfField() }}
                <button type="submit"
                    class="px-4 py-1.5 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition">
                    Logout
                </button>
            </form>
        @else
        <a href="{{ route('auth.login.show') }}"
            class="px-4 py-1.5 text-white border border-white/40 rounded-lg hover:bg-gray-500 transition">
            Login
        </a>
        <a href="{{ route('auth.register.show') }}"
            class="px-4 py-1.5 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition">
            Register
        </a>
        @endif
        </div>
    </header>

    <div class="flex min-h-screen pt-14 pb-10">

        <!-- Sidebar -->
        <div class="w-64 bg-gray-400 text-white shadow-lg p-6 flex flex-col fixed top-14 left-0 bottom-10 z-10">
            <div>
                <h2 class="text-2xl font-bold mb-8 tracking-wide">
                    FGLP Lead
                </h2>
                <div>

                <a href ="{{route('customer.index')}}" class="">Lead</a>
                </div>

               <div>         
               <a href="{{ route('admin.users.index') }}" class="">User</a>
               </div>

                {{-- Nav links go here --}}
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex justify-center ml-64">
            <div class="w-full max-w-7xl p-8">
                {{{ await $slots.main() }}}
            </div>
        </div>

    </div>

    <!-- Footer -->
    <footer class="bg-gray-400 text-white h-10 flex items-center justify-center fixed bottom-0 left-0 right-0 z-20">
        <p class="text-sm">&copy; {{ new Date().getFullYear() }} FGLP Lead. All rights reserved.</p>
    </footer>

</body>

</html>