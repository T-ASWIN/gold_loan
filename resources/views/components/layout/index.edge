<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrfToken }}">  {{-- 👈 add this --}}
    <title>Gold Loan System</title>
    @vite(['resources/css/app.css', 'resources/js/app.js','resources/js/customer/customer_form.js'])
</head>

<body class="bg-gray-100">

    <!-- Header -->
    <header class="bg-orange-200 shadow-sm h-14 flex items-center px-6 justify-between fixed top-0 left-0 right-0 z-20">
        <span class="text-black text-sm font-medium">FGLP Lead</span>

        <!-- Auth Section (right side) -->
        <div class="flex items-center gap-3 text-sm">
            @if(auth.isAuthenticated)
            <span class="text-black truncate max-w-xs">
                {{ auth.user.fullName ?? auth.user.email }}
            </span>
            <form action="{{ route('auth.logout') }}" method="POST">
                {{ csrfField() }}
                <button type="submit"
                    class="px-4 py-1.5 bg-orange-600 text-slate rounded-lg hover:bg-orange-700 transition">
                    Logout
                </button>
            </form>
        @else
        <a href="{{ route('auth.login.show') }}"
            class="px-4 py-1.5 text-slate border border-white/40 rounded-lg hover:bg-orange-500 transition">
            Login
        </a>
        <a href="{{ route('auth.register.show') }}"
            class="px-4 py-1.5 bg-gray-600 text-slate rounded-lg hover:bg-orange-700 transition">
            Register
        </a>
        @endif
        </div>
    </header>

    <div class="flex min-h-screen pt-14 pb-10">

        <!-- Sidebar -->
       <div class="w-64 bg-orange-200  text-white shadow-lg p-6 flex flex-col fixed top-14 left-0 bottom-0 z-10">
  <div>
    <h2 class="text-xl font-extrabold mb-8 tracking-tight text-black">
      FGLP Lead
    </h2>

    <div class="space-y-1">
      <a href="{{ route('customer.index') }}"
        class="block px-4 py-2.5 rounded-xl text-sm font-semibold transition-all
               {{ request.url().startsWith('/customer') ? 'bg-orange-600 text-black' : 'text-black hover:bg-orange-800 hover:text-black' }}">
        Lead
      </a>

      <a href="{{ route('admin.users.index') }}"
        class="block px-4 py-2.5 rounded-xl text-sm font-semibold transition-all
               {{ request.url().startsWith('/admin') ? 'bg-orange-600 text-black' : 'text-black hover:bg-orange-800 hover:text-black' }}">
        User
      </a>
    </div>
  </div>
</div>
        <!-- Main Content -->
        <div class="flex-1 flex justify-center ml-64">
            <div class="w-full max-w-7xl p-8">
                {{{ await $slots.main() }}}
            </div>
        </div>

    </div>

    <!-- Footer -->
    <footer class="bg-orange-200  text-slate h-10 flex items-center justify-center fixed bottom-0 left-0 right-0 z-20">
        <p class="text-sm">&copy; {{ new Date().getFullYear() }} FGLP Lead. All rights reserved.</p>
    </footer>

</body>

</html>